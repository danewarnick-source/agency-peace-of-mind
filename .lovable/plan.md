## Regenerate Supabase Types

Supabase types (`src/integrations/supabase/types.ts`) are auto-generated by Lovable Cloud after migrations are approved and applied. They cannot be regenerated by editing the file directly.

### What to do
1. Confirm the migration that added the new columns (e.g. `shift_medication_attestations`) has been **approved and run**. If it's still pending in the migration review panel, approve it — types regenerate automatically on success.
2. If the migration already ran but types look stale, trigger a refresh by re-opening the project or making a trivial no-op edit so the dev server reloads `types.ts`.
3. After that, I'll wire any code that depends on the new columns (remove `as never` casts in `punch-pad.tsx` and `dashboard.hhs-hub.$clientId.tsx` insert calls, type the `shift_medication_attestations` rows properly).

### If the migration was never created
The attestation feature's SQL currently only lives in `docs/SQL_HANDOFF.md`. If you want the types to include `shift_medication_attestations`, I should create a real migration for that table (CREATE TABLE + GRANTs + RLS + policies) so Lovable Cloud applies it and regenerates types. Want me to do that?