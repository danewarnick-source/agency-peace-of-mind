import { PiPublicHeader } from "@/components/pi-landing/pi-public-header";

/** Shared homepage marketing chrome. Do not invent a second public header. */
export function SiteHeader() {
  return <PiPublicHeader />;
}
