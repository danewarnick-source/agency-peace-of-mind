// Re-export for backwards compatibility. Canonical location is
// `./section-panel`, which encodes the shared client-profile section
// framing used across every tab.
export {
  SectionPanel,
  SectionGroup,
  SectionPanel as CareSection,
  SectionGroup as CareGroup,
  type SectionAccent,
} from "./section-panel";
