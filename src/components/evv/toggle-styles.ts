// Shared "selected vs unselected" pill/row styling used across the
// end-of-shift clock-out dialog so every pickable control looks the same.
export const selectedPill =
  "border-[color:var(--amber-600)] bg-[color:var(--amber-100)] text-[color:var(--navy-900)]";
export const unselectedPill = "border-border bg-background hover:bg-accent";
