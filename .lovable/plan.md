Replace `src/components/evv/punch-pad.tsx` (1249 lines) with the uploaded `punch-pad.tsx` (1239 lines) verbatim.

Steps:
1. Copy `user-uploads://punch-pad.tsx` → `src/components/evv/punch-pad.tsx` (overwrite).
2. Verify the file wrote cleanly (line count, imports, exported `PunchPad` + `PunchPadProps`).
3. Let the harness typecheck/build catch any compile issues; if it fails, report the errors and stop (no further edits without your go-ahead).

No other files will be touched.